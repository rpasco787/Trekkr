"""Setup file for Trekkr backend package."""

from setuptools import setup, find_packages

setup(
    name="trekkr-backend",
    version="0.1.0",
    packages=find_packages(),
    install_requires=[
        # Dependencies will be read from requirements.txt
    ],
)
